% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_llca.R
\name{plot.getfit.llcalist}
\alias{plot.getfit.llcalist}
\title{Plot fit indices across latent class solutions}
\usage{
\method{plot}{getfit.llcalist}(
  x,
  indices = NULL,
  title = "Model fit by number of classes",
  xlab = "Class solution",
  ylab = "Value",
  base_size = 12,
  colors = NULL,
  pch = 19,
  lwd = 2,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"getfit.llcalist"}, normally returned by
\code{getfit()} for an \code{llcalist} object.}

\item{indices}{Character vector containing the names of the fit indices to
plot. When \code{NULL}, the function uses \code{AICp} and \code{BICp}
for penalized models and \code{AIC} and \code{BIC} otherwise.}

\item{title}{Character string with the plot title.}

\item{xlab}{Character string with the horizontal-axis label.}

\item{ylab}{Character string with the vertical-axis label.}

\item{base_size}{Numeric value controlling the overall text size.}

\item{colors}{Optional character vector of line colors. Colors are recycled
when fewer colors than indices are supplied.}

\item{pch}{Plotting symbol used for the fitted values.}

\item{lwd}{Width of the lines connecting fitted values.}

\item{...}{Additional arguments. Currently unused.}
}
\value{
The plotted data, invisibly.
}
\description{
Creates a scree-style base R plot comparing information criteria across the
models contained in a fitted latent class enumeration.
}
\details{
Each selected information criterion is represented by a separate line. The
horizontal axis gives the number of latent classes and the vertical axis
gives the value of the selected criterion.
}
\examples{
\dontrun{
fits <- lca(data = gss82, nclasses = 1:5,
            multinomial = c("PURPOSE", "ACCURACY"))
fit_indices <- getfit(fits)
plot(fit_indices)
plot(fit_indices, indices = c("AIC", "BIC", "SABIC"))
}

}
