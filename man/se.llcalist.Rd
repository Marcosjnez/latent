% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_llca.R
\name{se.llcalist}
\alias{se.llcalist}
\title{Standard errors for a collection of latent class models}
\usage{
\method{se}{llcalist}(model, type = "standard", digits = 3L, ...)
}
\arguments{
\item{model}{An object of class \code{"llcalist"} containing fitted
\code{"llca"} objects.}

\item{type}{Character string indicating the standard-error estimator. See
\code{se.llca()}.}

\item{digits}{Non-negative integer indicating the number of decimal places used
in the formatted tables, or \code{NULL} to avoid rounding.}

\item{...}{Additional arguments passed to \code{se.llca()}.}
}
\value{
A named list with one standard-error result per fitted model and class
  \code{"se.llcalist"}.
}
\description{
Applies \code{se()} to every fitted model in an \code{"llcalist"} object.
}
\details{
Existing names are preserved. Consequently, class-enumeration results retain
names such as \code{"nclasses=2"}, whereas multiple-step models retain names
such as \code{"measurement"} and \code{"structural"}. Unnamed elements are
labelled according to their number of latent classes.
}
\examples{
\dontrun{
fits <- lca(data = empathy, nclasses = 2:4,
            gaussian = c("ec1", "ec2", "ec3"))
se(fits)
}

}
